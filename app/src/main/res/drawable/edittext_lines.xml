<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/darkblue" /> <!-- Background color, change as needed -->

    <stroke
        android:color="@color/black"
        android:dashWidth="1dp"
        android:dashGap="2dp" />

</shape>
